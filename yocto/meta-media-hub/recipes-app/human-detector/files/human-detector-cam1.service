[Unit]
Description=Human Detector Service (Camera 1)
After=network-online.target ffmpeg-stream.service
Wants=ffmpeg-stream.service

[Service]
# Load credentials from the private environment file
EnvironmentFile=/etc/detector.env

# Use the variables from the file
ExecStart=/usr/bin/python3 /usr/bin/human_detector.py \
    --stream "tcp://127.0.0.1:9090" \
    --camera-id "camera1" \
    --broker "${MQTT_BROKER}" \
    --port "${MQTT_PORT}" \
    --user "${MQTT_USER}" \
    --password "${MQTT_PASSWORD}"

Restart=always
RestartSec=10
User=root

[Install]
WantedBy=multi-user.target